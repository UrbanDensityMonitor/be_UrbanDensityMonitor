
services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: urban-density-backend:latest
    container_name: urban_density_backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - ../.env
